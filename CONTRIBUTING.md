# 参与贡献

感谢你愿意改进 Codex Remote。项目目前面向单用户、自托管使用，提交功能前请保持
这个边界清晰；多人账户或托管服务属于明显更大的设计范围，建议先开 Issue 讨论。

## 开发检查

```bash
npm ci
npm run typecheck
npm test
```

涉及真实 Codex App Server 的改动，还应在自己的测试项目中运行相应 smoke 测试和
浏览器验收。不要让测试连接包含重要文件的正式项目。

## 提交内容

- 保持改动聚焦，并为行为变化补充测试；
- 不要提交令牌、真实项目路径、会话内容、主机名或带私人信息的截图；
- 修改网络入口时，继续保持应用只监听 `127.0.0.1`；
- 修改权限行为时，保持受限默认值，不要静默启用 full access；
- 不要手工大批修改 `src/generated/`，应使用对应 Codex CLI 重新生成并记录版本；
- 新增 PWA 静态文件时，同步更新静态白名单、缓存版本和测试。

提交贡献即表示你有权提供相关代码，并同意它按仓库的 MIT License 发布。
