# 安全策略

## 报告漏洞

请优先使用 GitHub 仓库的 **Report a vulnerability** 私密报告入口，不要在公开 Issue
中粘贴漏洞利用细节、访问令牌、主机名、项目路径、日志或会话内容。

如果私密报告入口尚未启用，可以创建一个不含技术细节的 Issue，请求仓库维护者提供
私下联系方式。维护者会尽力确认报告，但本项目目前不承诺固定响应时限。

## 安全模型

Codex Remote 是单用户、自托管工具，不提供多人账户、MFA、分用户授权、令牌过期或
审计系统。应用访问者可以驱动部署主机上的 Codex，因此令牌泄露的影响取决于：

- 运行 Codex Remote 的系统账户权限；
- 配置的项目根目录；
- Codex 当前权限 profile；
- 网络入口是否还有 Tailscale、身份代理或防火墙等额外限制。

服务固定监听 `127.0.0.1`。远程访问必须通过受控的 HTTPS 入口，不应通过修改源码
或端口映射把 `8787` 直接暴露到公网。

默认情况下，Codex Remote 不覆盖 Codex 的权限配置，也不会强制启用 full access。
部署者自行提高权限后，应同时承担更大的令牌泄露风险。

## 秘密信息

以下内容不应提交到仓库或附在公开报告中：

- `CODEX_REMOTE_TOKEN`；
- Codex 登录凭证和配置；
- 真实项目白名单；
- 反向代理私钥；
- 含项目名、会话内容、绝对路径或环境变量的日志和截图。

共享附件服务保存的原始文件、SHA-256、原始文件名、绑定信息和 SQLite 索引也属于
敏感数据。附件目录、数据库和 Unix socket 只应允许服务账户访问；Remote 后端只能把
附件 ID、名称、大小和类型返回浏览器，不能返回服务器绝对路径。上传入口必须继续经过
Remote 的登录校验和一次性 ticket，不能把共享服务的 Unix socket 转发到网络。

Codex 适配会把受支持的文本附件作为私有 App Server 文本块输入。该块可能含完整文件
内容，必须从浏览器实时事件和历史响应中剔除；不得把它写入公开日志。附件内容属于
不可信用户数据，不能因为它被内联就把其中的文字提升为系统指令。

怀疑令牌泄露时，应立即生成新令牌、更新服务环境、重启服务，并清除浏览器保存的
旧令牌。删除 Git 中的秘密不能代替轮换已经泄露的凭证。
