# 留空时使用服务账户 HOME 下的 ~/.local/share/ai-remote/。
# AI_REMOTE_UPLOAD_ROOT=/var/lib/codex-remote/ai-remote/uploads
# AI_REMOTE_UPLOAD_SOCKET=/var/lib/codex-remote/ai-remote/upload.sock

# 低于这个可用空间时暂停新上传。0 表示只保留操作系统本身报告的可用空间。
AI_REMOTE_UPLOAD_MIN_FREE_MIB=1024
